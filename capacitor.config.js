/** @type {import('@capacitor/cli').CapacitorConfig} */
const config = {
  appId: 'com.doom3d.arena',
  appName: 'Doom3D Arena',
  webDir: 'dist',
  backgroundColor: '#07070d',
  android: {
    allowMixedContent: true
  }
};

module.exports = config;
